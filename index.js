

const main = () => {
    console.log("i'm in main 2!");
    // Initial setup.
    // Start payrolldetails.

    // insert_punchces()
    // myCrons = [PunchesCron(), ScheduleCron(), ...];
    // for (cron in myCrons){
    //     cron.Run();
    // }
}

try {
    main();
} catch (error){
    alert(error)
}

const alert = () => {
    //send email
}